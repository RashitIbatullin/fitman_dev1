import 'package:flutter/material.dart';

class TimesheetPage extends StatelessWidget {
  const TimesheetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Табели'),
      ),
      body: const Center(
        child: Text('Табели - в разработке'),
      ),
    );
  }
}
